{
  "name": "yt-playlist-downloader",
  "version": "1.0.0",
  "private": true,
  "description": "Web app para baixar playlists e vídeos do YouTube (estilo 4K Video Downloader)",
  "scripts": {
    "install:all": "npm --prefix backend install && npm --prefix frontend install",
    "dev": "concurrently -n backend,frontend -c blue,magenta \"npm --prefix backend start\" \"npm --prefix frontend run dev\"",
    "build": "npm --prefix frontend run build",
    "start": "npm --prefix backend start"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
